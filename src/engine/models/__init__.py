"""The intermediate representation (spec section 4).

Pydantic v2 models here are the only definition of the IR; the JSON Schema under `schema/` is
generated from them (NFR-MNT-01). Removing or renaming a field is a schema version change.
"""
